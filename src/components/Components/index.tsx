import { CustomButton } from '../shared/CustomButton';

export const Components = () => {
  return (
    <>
      <p>button</p>
      <CustomButton text={'Click me'} onClick={undefined} color={'danger'} />
    </>
  );
};
